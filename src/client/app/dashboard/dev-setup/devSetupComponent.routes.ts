import { Route } from '@angular/router';
import { DevSetupComponent } from './devSetup.component';

export const DevSetupRoutes: Route[] = [
	{
		path: 'setup',
    component: DevSetupComponent
	}
];
